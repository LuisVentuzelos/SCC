apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: viternetwork
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/viternetwork: "true"
  podSelector:
    matchLabels:
      io.kompose.network/viternetwork: "true"
